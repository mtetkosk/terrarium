[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (requires real LLM API calls)
addopts = -v --tb=short -m "not integration"

